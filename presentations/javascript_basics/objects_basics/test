
[title Working With Objects]
[execute index.html]
[time 4]

[explanation source:explanation.md]

## Update the following object

<pre class="code javascript" >
var user = {
  name: 'Fred Smith',
  admin: true
};
</pre>

* _add_ properties for `first` and `last` name (using 'Fred Smith')
* _remove_ `name` property
* _assign_ to `email` with the value **'fred@sample.com'**
* _change_ the `admin` property to **false**

[zone name:script.js for:script_objects syntax:javascript]
[test source:test.js]
