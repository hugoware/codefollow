
[title]
Type Checking

[execute index.html]
[time 3]

[explanation]

## Declare four functions

* `is_number` that accepts an argument and returns if it's a **number**
* `is_string` that accepts an argument and returns if it's a **string**
* `is_array` that accepts an argument and returns if it's an **array**
* `is_user` that accepts an argument and returns if it's a **User type**

_`User` is a custom type for this demo._

[zone name:script.js for:types syntax:javascript]
[test source:test.js]
