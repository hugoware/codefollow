
[title]
If/Then

[execute index.html]
[time 3]

[explanation]

## Declare 5 variables

* _variable_ named `a` with the value **3**
* _variable_ named `b` with the value **false**
* _variable_ named `c` with the value **'name'**
* _array_ named `d` with the values **'a', 'b', and 'c'**
* _object_ named `e` with a property **color** and the value **blue**

[zone name:script.js for:if_then syntax:javascript]
[test source:test.js]
