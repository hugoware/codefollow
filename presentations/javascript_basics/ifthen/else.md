# If/Then
## Else

<pre class="code javascript" >
if ( false )
  console.log('nope')
else
  console.log('yep')

// output: 'yep'
</pre>

* `else` will execute it's block if no other preceeding conditions are met